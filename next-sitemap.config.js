/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://calebluebbering.com', // replace with your real domain
  generateRobotsTxt: true,           // (optional) generates robots.txt
  priority: 0.8,                     // optional
  sitemapSize: 5000,
};